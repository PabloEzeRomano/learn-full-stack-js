'use strict';

'use strict';

export default ngModule => {
  describe(`company-view`, () => {
  
    beforeEach(window.module(ngModule.name));
    
    it(`should test company-view`, () => {
      expect(true).to.be.true;
    });

  });
};